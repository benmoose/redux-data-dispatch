{
  "name": "redux-data-dispatch",
  "version": "0.2.2",
  "description": "Define data dependencies and links between reducers.",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib",
    "pretest": "npm run build",
    "test": "jest",
    "preversion": "npm test",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "redux",
    "dependencies",
    "data",
    "state-management"
  ],
  "author": "Ben Hadfield <benjohnhadfield@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^21.2.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "jest": "^21.2.1",
    "redux": "^3.7.2",
    "redux-mock-store": "^1.3.0",
    "standard": "^10.0.3"
  },
  "peerDependencies": {
    "redux": "^3.0.0"
  },
  "standard": {
    "ignore": [
      "lib/*"
    ]
  }
}
